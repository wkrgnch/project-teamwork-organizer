
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project_teamwork_organizer
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8081

